@model MAX_KID.Models.Entities.LOAISP

@{
    ViewBag.Title = "Create";
    Layout = "~/Areas/ADMIN/Views/Shared/_Layout.cshtml";
}

<h2>Create</h2>

@using (Html.BeginForm("Create", "LOAISPADMIN", FormMethod.Post, new { enctype = "multipart/form-data" })) 
{
   @Html.AntiForgeryToken()
    
    <div class="form-horizontal">
        <h4 style="color:#8b0a0a">THÊM MỚI LOẠI SẢN PHẨM</h4>
        <hr />
        @Html.ValidationSummary(true)

        <div class="form-group">
            <label class="control-label col-md-2" for="ID_DM">TÊN LOẠI SẢN PHẨM</label>
            <div class="col-md-10">
                @Html.EditorFor(model => model.TenLoaiSP)
                @Html.ValidationMessageFor(model => model.TenLoaiSP)
            </div>
        </div>

        <div class="form-group">
            <div class="col-md-offset-2 col-md-10">
                <input type="submit" value="THÊM MỚI" class="btn btn-default" />
            </div>
        </div>
    </div>
}

<div>
    @Html.ActionLink("QUAY VỀ TRANG TRƯỚC", "Index")
</div>
